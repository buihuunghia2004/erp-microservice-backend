export enum Gender {
  MALE = 'male',
  FEMALE = 'female'
}

export enum EmployeeRole {
  ADMIN = 'admin',
  MANAGER = 'manager',
  EMPLOYEE = 'employee',
}

export enum Nationality {
  VIE = 'viet_name',
  CAM = 'cambodia',
  ENG = 'english'
}

export enum PlanActivityASMType {
  DEFAULT_USER = 'default_user',
  MANAGER = 'manager',
  COACH = 'coach',
  EMPLOYEE = 'employee',
  ASK_AT_LUNCH = 'ask_at_lunch',
}

